#!/bin/sh
# CSI Guard hook — Cybernetic Steering Invariant. Reliably-in-path: runs on every commit.
# Self-run, no external authority (the engine enforces itself). The mechanical error steers.
#
# Activate once per clone (tracked hooks, not .git/hooks):
#   git config core.hooksPath dialectic/githooks
exec python3 dialectic/guards/anchor_guard.py
