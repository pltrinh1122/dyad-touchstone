# Ledger — cycle #7: b3 written; the candidate Playbook lives; a stale-cache note self-heals

## Date
2026-06-03

## The claim under test
`b3` can be climbed — `library/dag-planning/PLAYBOOK.md` synthesized from the lived
ledger — and the leave adjudicated **by query**, not narration. And: is `b3` actually
"Founding-gated," as cycles #5–#6 asserted?

## The refutation it produced (the point of the cycle)
Cycles #5 and #6 both recorded the frontier as **"b3 (PLAYBOOK.md, Founding-gated) —
deferred."** Rubbing the substrate this run **refuted it**: `dag.yaml` gates `b3` on
`b4` *only* (a Playbook with no ledger is not yet a Playbook), and `b4` is done — so
the Touchstone reports `b3 = READY`. **No Founding gate is encoded anywhere in the DAG.**
The "Founding-gated" line was a stale-cache annotation, propagated forward into #6 by
narration. The summit caught its own ledger — *un-refuted ≠ correct*, demonstrated again
on our own record. This cycle is the fresh rub that heals it (history not rewritten;
re-derived).

## What was reconciled (pinned this cycle)
- **live → write → share** (Anchor single-home discipline #6): a discipline matures
  through three touchstone-gated leaves. The **Founding gate sits at `write → share`, not
  before.** Writing the *internal* Playbook is **living, not sharing** — public-readable in
  our own repo ≠ proposed-to-the-Commons. So `b3` was never gated; it is the *write* stage.
- "Share" = `pin` at the Commons scale: same protocol, bigger blast radius, higher
  soundness bar (`pin-protocol`, Anchor D4).

## What was attacked, and survived
- **Playbook grounded, not narrated.** Before writing, all six cycles + the Commons
  canonical format (`CONTRIBUTING.md`, `library/proposal-framing/`) were re-read *this run*;
  the prose is sourced from proofs and shaped to be shareable (low-friction at the future
  `write → share` leave). d5 applied to the Playbook about d5.
- **The leave was queried.** Writing the file dropped `b3 → done` and emptied the frontier
  (all terminals done) with **no code or flag change** — cycle #1's capstone discipline,
  re-run.
- **Scope held.** The Playbook stays on the dag-planning discipline (its single home); the
  session's marker lexicon is named a *sibling* instance, not annexed.

## State
Frontier: **empty — all terminals done.** The candidate Playbook is **written (internal)**:
`library/dag-planning/PLAYBOOK.md`, ledger referenced at `dialectic/ledger/` (single-home).
Remaining leave: **`write → share`** (Founding gate) — deferred, gated by lived soundness,
not asserted.

## Caps (no silent truncation)
Single-dyad ledger (dyad-touchstone, n=7); admission to the Commons provisional, deferred
to the share leave. Thrash-detection still unimplemented; predicate vocabulary still minimal.
