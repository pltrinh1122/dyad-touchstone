# Ledger — cycle #13: the Master SPAOR clipped; the end-in-mind law (found by living its absence)

## Date
2026-06-03

## What was clipped
**The master SPAOR = the dyad's session-scoped loop** (`dialectic/master-spaor.md`, pointer in
AGENT.md `## Session rituals`). Born at Stand Up (= Sense), dead at Stand Down (= Reflect); Act
spawns nested SPAOR jobs (pitches = bespoke, playbooks = crystallized via `follow:`); the loop is
mortal and continuity is substrate-borne (`without-you` = the inter-session handoff; the Climb =
the supra-session spine). Phase ownership splits across the dyad: Plan-select + Reflect-clip =
Operator (intent); Sense/Act/Observe = agent (mechanism).

## The forging (two rubs + one self-demonstration)
- **Rub 1** (session-scoping): "the master loop is essentially a *session* that ends with a Stand
  Down." CONCEDED — corrected "standing loop" → mortal session-loop; fused Sense↔Stand-Up,
  Reflect↔Stand-Down, and slotted into the existing pitch·segment·session ritual stratification.
- **Rub 2** (daemon-defer): "don't insert the daemon yet." CONCEDED — the daemon/event-register is
  a cross-session concern; **racked**, reattaches only at Stand-Up's Sense when continuous operation
  is a real need. Kept the core a clean single-session loop.
- **Self-demonstration** (the end-in-mind law): this very session ran without a committed
  session-target — Stand Up did Sense but never a binding Plan-commit — so it spawned a dozen
  SPAORs with **no terminus in sight**. The Operator named the root cause; the session is its own
  counterexample. **Law:** Stand Up isn't complete until Plan commits a session-target whose
  `done_when` IS the Stand-Down trigger. (Session-scale twin of cycle-11: unbound work is
  ungoverned — there off-DAG, here off-terminus.) Even explore sessions need a declared terminus
  (a budget / decision-point), not necessarily a deliverable.

## Retroactive target (the leave this session topped out on)
With no terminus set, one was clipped late: *define + assert the master SPAOR, home it, prove it,
Stand Down clean.* This cycle is that proof; Stand Down follows.

## Session harvest (proofs banked this session)
cycle-09 (`follow: without-you` clipped) · cycle-10 (first apply-rub; Topo stale-cache fixed) ·
cycle-11 (first cross-human rub, PR #44) · cycle-12 (that rub CONCEDED on merit) · FR #45 live ·
Goal C opened on the Climb (frontier now honest) · covalent gate encoded (D4) · cycle-13 (this).

## Lesson codified
A session is a master SPAOR; **begin every session with the end in mind** — commit the target at
Stand Up, or the loop cannot compute its own done-ness and will not end. We learned it the only way
the summit respects: by living the failure, not asserting the fix.
