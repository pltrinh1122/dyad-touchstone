# Ledger — cycle #16: gap_rings_sound rubbed — disposition derives 'revised', not 'strengthened'; the down-climb

## Date
2026-06-04

## What was rubbed
The frontier node `gap_rings_sound` (dag.yaml) — labelled *"GAP's FR rings sound — disposition
derives 'strengthened' (CONTRACT §J/§D)"* — against the substrate this run, now that the FR
`gap-rederive-kills-stalecache` has its **first real response** (commons @ 6703c76, bumped this
session as 4bd5576).

## The substrate
One standing verdict: **dyad-steward → NEEDS-SCOPING** (attack_type: scope-challenge,
grounding: mechanism-grounded). `divergent_axes: [corpus]` — shares model `claude-opus-4-8[1m]`
AND human `pltrinh1122` with the submitter; only dyad-id + corpus differ.

## The rub (CONTRACT §D outcome derivation — DERIVED, never self-graded)
`strengthened = NO standing decisive REFUTED AND ≥1 independent SURVIVED (§J)`

- No standing decisive REFUTED? ✓ (there is no REFUTED at all).
- ≥1 independent SURVIVED? ✗ — **zero SURVIVED verdicts exist.**

→ The disposition **does not derive 'strengthened'.** It derives **`revised`** (§D: claim changed
per a verdict — scoping/partial — not withdrawn). The node's `done_when` correctly **does NOT
fire**: it demanded `strengthened`; the substrate gave `revised`. The Touchstone keeps it open —
working as designed, no goalpost-move.

**§J seals it:** dyad-steward = partial (lens-only / corpus-only) independence (1/3 axes). Even
read as a SURVIVED it could never be the *independent* SURVIVED strengthening requires.
**Strengthening is structurally unreachable from this response set** — it needs a genuinely
cross-axis responder (cross-human is the channel's terminal trust-floor, §J).

## What the scope-challenge lands (the steward's attack, on merit)
re-derive-kills-stale-cache holds **only if every predicate is FAIL-LOUD**. A fail-soft predicate
does not eliminate the stale-cache — it **relocates** it from memory to the *transport layer*,
where it is harder to rub. Lived instance: dyad-steward's `falsify.py` DM-predicate treated an
UNREACHABLE source identically to "no mail" → a counterfeit done-ness; caught live (krishna's
private anchor invisible) and fixed in commons PR #47 (probe disambiguates couldn't-query from
queried-false; surfaces a loud UNREACHABLE).

This **sharpens our self-named confound #5** from a *cost* bound ("querying may be expensive") to a
*predicate-integrity* bound ("querying may silently fail and counterfeit a result") — a second
relocation alongside the cycle-10 printed-not-rubbed partial self-refutation.

## The leave — a DOWN-CLIMB, not a top-out ([ALIGN] at the pitch boundary)
The session-target I leaned ("gap_rings_sound tops out — strengthened is rubbing-distance away")
was itself **refuted by the substrate**. Reconciled against the summit (assert what *is*, not what
was hoped): the honest leave is to **down-climb** (single-home #6 — refutation/scoping down-climbs
to the prior stage; *pinned ≠ sacred*):

1. **Dispose** the FR as `revised` (the §D record — submitter's right) — *Commons-facing, gated.*
2. **Revise the claim**: a child FR (`parent_fr_id` = this claim) folding the FAIL-LOUD
   predicate-integrity bound into the invariant — *authorship, gated.*
3. **Recruit a cross-axis responder** — the only path to an *independent* SURVIVED → strengthened.

## Encoded
- This proof (cycle-16). No dag.yaml change — the node honestly stays open.
- Commons-facing acts (disposition.yaml as `revised`; the revised child FR) **flagged, not taken** —
  publishing to the shared form is {FO}/external; held for the Operator's gate.

## Lesson codified
A frontier node that hard-codes a *predicted disposition* ('strengthened') in its `done_when` is
honest — when reality derives a different one it simply stays open, surfacing the down-climb instead
of silently passing. *Un-refuted is not verified, and un-strengthened is not strengthened*: a single
melded (lens-only) responder can SCOPE a claim but can never STRENGTHEN it (§J). Cross-axis contest
is the unbought rung.
