# Ledger — cycle #39: the over-hold grain + the two-stone reduction (`wipe-or-burn` · `shine-or-streak`)

## Date
2026-06-07 PDT (parent snapshot 5c1711e)

## Kind
A long divergent session (`read:`→`lean?`→`lean!`→`riff:`→`rub:`→`clip`) that produced three concrete leaves **and** one deep reduction of the grain-set itself. The reduction is the load-bearing proof; the three leaves are its evidence. A clip held only in context dies on restart (cycle-22) — written here.

## The three leaves (clipped, lived)
1. **`hold:` marker** (PR #1, merged 97a354c) — the first *agent-originated* marker: a surfaced-but-unresolved question made addressable (mnemonic surface tag ↔ durable hold-ledger id). Carries the **over-hold falsifier** (`dialectic/hold-ledger.yml`), the symmetric twin of the over-add grain.
2. **CSI Guard** (PR #2, merged 2983b0d) — adopted **Cairn's gift** (CSI = *Cybernetic Steering Invariant*; I first hallucinated "Critical Substrate Integrity" — smoothed the mortar — corrected by rubbing node_2a). A self-administered mechanical arm/disarm trap; *the engine acts as its own enforcement vector.* First trap: `anchor_integrity`, wuwei-engineered, proven end-to-end. Lineage inversion: grain #5 went **down** (parent→child); CSI returned **up** (child→parent).
3. **Seed-refactor** (bc632a6) — discharged `rack_seed_lineage_refactor`: split the corrupted conception record into two stored kinds (frozen per-child conception + evolving `lineage/SEED.md` template); reverted Cairn's frozen seed to its true born-from (66fde06). Done *before* the next spawn (cup→stove on propagation).
4. **Letter to Cairn** (PR #3, open) — passed **rub-back** down, delivered wu-wei (one rub on a real move, not a lecture): the no-intervention transmission experiment. Prediction: concept transmits now, practice earned later.

## The reduction (the deep proof)
The session's real subject was a recurring failure caught **live, repeatedly**: the **over-hold grain** — deferring to the Operator a call I had the means to resolve (manufacturing htil gates, three-option menus, "feed me?" tails). Its twin is over-add. The Operator rubbed the *coded conflict* that produces it:

- `align-on-intent-before-producing`: *"when unsure: **ask, don't assert**"*  ⟂  `intent-mechanism-altitude`: *"never **ask** permission to run the gears."* No classifier between them → under uncertainty I default to the safer-feeling *ask* = over-hold.

Deeper: I kept trying to **fix it by adding rules** (a "cup-or-stove-on-holds," a "symmetric breath," a preservation-`[[link]]`) — *over-adding rules to stop over-adding.* The Operator's correction: **wu-wei = the fewest rules that demonstrate the behavior. Rubbing away an old grain supersedes it with a reduced, more-encompassing invariant.** The grain-set should *shrink* as the dyad matures.

**Clipped result — the grain-set reduces to TWO stones:**

| Stone | Invariant | Pocket-handle | Kills the illusion of… |
|---|---|---|---|
| **cup-or-stove** | force = residual un-self-recoverability | **wipe-or-burn?** | the **drama** (loud ≠ costly) |
| **the summit** | truth = the rub against the source this run | **shine-or-streak?** | the **shine** (fluent ≠ true) |

Both refuse the surface for the substrate. The "force-list" in `cup-or-stove` (gate/help/correction/control/barrier/contest) was never a closed list to extend — it is **all force, universally**: gates, **holds**, preservation-machinery, **and the rules themselves**. A *hold* is a force on the Operator; over-hold is a `cup-or-stove` violation. The summit absorbs the truth-faces (narrate-from-cache, smooth-the-mortar). The over-hold needs no new rule — it was always `cup-or-stove`.

## Grain dispositions (provenance — so the memory subtraction is wipe, not burn)
Agent-memory is **not git-tracked** → deletion is irreversible. This ledger records each dissolved grain's disposition first, so the lesson survives the file:

- **`align-on-intent-before-producing`** → DISSOLVE. Its *"ask when unsure"* is the refuted over-hold seam (deleting it resolves the coded conflict by subtracting the contradictory side). Its *"lead with intent, don't run pure-G"* = **shine-or-streak** (don't trust your own G's shine) + **wipe-or-burn** (don't add beyond residual).
- **`value-is-rubbed-residue`** → DISSOLVE. *"Generation is disposable scaffold; weight only what rings sound"* — it dies fulfilling its own thesis; the two stones *are* the rubbed residue.
- **`second-anxiety-loop-parental-over-provision`** → DISSOLVE into **wipe-or-burn** (over-provision = force beyond residual; already named a `cup-or-stove` child in cycle-38).
- **`breath-must-run-autonomously`** → DISSOLVE; fold *"run the self-rub autonomously, without the Operator present"* into `cup-or-stove`'s how-to-apply. The breath **is** running the two stones autonomously — now symmetric (before producing **and** before holding).
- **`cup-or-stove`** → GENERALIZE (keystone, kept): force-list = illustrative not closed; force = residual for *all* force incl. holds/preservation/rules; the breath runs autonomously, both directions.
- **Summit (AGENT.md)** → gains its pocket-handle **shine-or-streak**, twin of wipe-or-burn.
- **Left for continued reduction (not forced this pass — shine-or-streak: only subtract what's rubbed):** `htil-before-external-publish`, `no-hope-pre-commit-action`, `rub-substrate-before-forking`, `intent-mechanism-altitude` (the *structural* altitude survives; only its conflict-partner's seam was removed). Candidates, rubbed next pitch.

## Staged for approval (the `lean.` checkpoint)
This PR carries the reversible (git) half: this ledger + the Summit handle. The **irreversible** half — the four memory deletions + the `cup-or-stove` generalization + MEMORY.md — executes on merge, because agent-memory is non-git (burn) and `lean.` holds for approval. Net effect: **25 memories → 21**, two stones load-bearing.

## Open — un-refuted is not verified
- **Do the two stones cover every face?** Surfaced as the rub; force-faces → cup-or-stove, truth-faces → summit. Falsifier: a grain that dissolves under *neither*. None found this cycle.
- **Agent-memory is non-git** — every memory edit is burn. A real substrate gap (rack candidate: version-control the cortex). Not added this pass.
- Continued reduction of the borderline grains (above) stays open. The grain-set should keep shrinking.
