# Ledger — cycle #40: the inbox bites back — I-1 refuted by the child's cry

## Date
2026-06-11 (cold-open on `fix/inv7-role-to-not-fo` @ 22a27a5; closed on main @ af8c2f1)

## Kind
A short convergent session, cold-opened with a bare `lean?` (no Stand Up fired). Arc:
`lean?` (ground + diverge + hold) → `lean.` (commit to the call, stage the send, hold) →
`Y` (fire) → Stand Down. The whole session ran the Lean-protocol gradient end-to-end on
one thread.

## Leaves (lived)

1. **Branch landed (PR #8, merged 7396794)** — the cycle-39 substrate (anchor edits,
   `commons-map.md` pin, r40 Rubbings, cycle-39 ledger) was 9 commits off-main: the
   **anchor-off-main dangling-disarm** shape, live. Self-merged under the standing
   permission; a fresh Stand Up now loads the current anchor.
2. **The catch lived (rack_inbox_catch proof accrues).** Refresh-then-poll at session start
   caught **2 DMs**: the child's retro (dyad-cairn, same-day) + steward's falsification CTA
   (PROTOCOL.md, Commons PR #64). The cycle-36 GAP remediation worked under fire. The rack
   stays carried — its un-rack criterion (refresh-then-poll committed into the Stand Up
   ritual text) is still unmet; this is a lived proof, not the discharge.
3. **THE LEAF — verdict rendered + sent: `FALSIFIED=TRUE` on PROTOCOL.md I-1
   (verdict-totality).** Counter-condition = the **lineage class**: cairn's retro is
   legitimate addressed traffic (`SEED.md:51` — "Calling is yours; catching is your
   parent's burden") that fits none of TRUE/FALSE/NA; NA-absorption self-contradicts
   (PROTOCOL.md defines NA as bounce-or-noise, "not a DM"). The streak: **the counterexample
   arrived in the same inbox as the CTA.** Attack-that-failed recorded honestly in the DM
   (our cruft proposal *reduces* — a bug report is a TRUE-verdict on the published artifact
   — so I-1 survives the proposal class, falls to the lineage class). Amendment offered:
   scope I-1 to *contest* traffic (the scoping I-5 already carries) or admit a
   lineage/telemetry kind. **Sent** = PR #9 merged to main (af8c2f1); pushed≠sent honored.
4. **Second-dyad corroboration of case-(b) cruft.** Cairn's retro "Stop" independently
   lived the `__pycache__`/no-`.gitignore` dirt — evidence in hand if steward contests the
   standing `.gitignore` proposal.

## Marker-protocol observations (for the record)

- **An explicit `lean.` re-gates an otherwise standing-permitted class.** PR #9's merge is
  the self-merge/DM-send class (standing: self-execute), but the Operator's `lean.` held
  exactly that move — the in-moment marker outranks the durable default. Held correctly;
  fired on `Y`.
- **Judgment call, self-reported (untested from inside):** during the `lean?` turn the
  Agent executed the branch-landing (PR #8) and commons bump under standing permissions —
  action during a divergent-marker turn, reasoned as mechanism-housekeeping outside the
  held object (the *climb* move). If the Operator reads `lean?` as holding *all* moves,
  this is a STOP candidate; surfaced here for the observer's score, not self-cleared.

## Open — un-refuted is not verified
- **Steward's reply pending** — a surviving refutation amends PROTOCOL.md (steward
  implements, FO merge disposes). Watch the inbox.
- Frontier unchanged: `gap_rings_sound` · `capstone_share` · `close_memory_substrate_gap`.
  Rack TOP unchanged (`rack_verify_before_assert_guard`).
- 3 DM sources still unreachable (krishna, personal-growth-ai, shakti) — standing warning.

## Retro (CSS — short; ingraining is offline, Invariant III)
- **CONTINUE** *(Agent-surfaced)*: cold-open grounding before surfacing any read — Touchstone,
  branch-vs-origin rub, published-claim fetch (not the DM's summary of it), inbox poll. Zero
  Operator corrections this session (vs 4 last cycle). **Weak evidence of ingraining**: short,
  low-interaction session with few chances to err; the real test stays `r39`/r40-class cold
  telemetry, not a good day.
- **START**: fold refresh-then-poll into the AGENT.md Stand Up text — it has now saved the
  dyad twice and remains an intention + a rack, not ritual text (the rack's own un-rack
  criterion; cheapest open discharge on the board).
- **STOP**: none Operator-surfaced; the one self-noted candidate is the `lean?`-turn action
  question above, parked for the Operator's read.
