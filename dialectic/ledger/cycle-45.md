# Ledger — cycle #45: re-rub the `d-*` trigger family against dyad-bond; collapse Intermission

## Date
2026-07-06 (session). Triggered by an Operator arc of borrows: `d-start` (*"borrow and implement our
own equivalent"*) → `d-reflect` (*"borrow and land d-reflect as well"*) → two `raff:`s collapsing
Intermission → a `d-rub` on Invariant III → `riff:`→`raff:` adopting bond's `d-land` drift-sweep.
De-facto session-target: **bring touchstone's `d-*` trigger family up to bond's current forms, recast.**
Topped out on this `d-reflect`.

## Kind
Mixed harvest. **Two rubbed-and-held proofs banked** (below). **Three borrows landed but un-rubbed**
(Frozen, n=1) — `un-refuted ≠ verified`, so they are *not* banked as proofs, only recorded as living
candidates. One **caught failure** logged (the Invariant III drift). `pinned ≠ sacred`.

## Leaves (lived, rubbed, held — the proofs)

1. **Intermission is not a primitive ritual — it is `d-reflect` fired at a segment seam, duplicative
   under constant durability.** RUBBED across two turns: I first falsified the *weaker* Operator claim
   (*"constant durability retires Intermission"*) by grounding Intermission against the substrate —
   `master-spaor.md:24` + `DYAD.md` rituals table showed it is a **Reflect→Sense re-entry** (harvest +
   re-derive), not a durability trigger. The Operator then *sharpened* to the sound core — *"identical
   Agent-actions ⟹ duplicative"* — and that HELD: Intermission's actions = Stand-Down's Reflect ∪
   Stand-Up's Sense; the one differentiator (context stays live vs. teardown) is **not an Agent-action**,
   so it does not save the ritual as a distinct action-set. Collapsed (`8e350e9`); adopts bond's
   *"d-reflect fires mid-session or at session-end alike,"* **reversing** this session's own earlier
   decision to keep them distinct — correction as fuel, the Operator's arc drove it.
   *Residual preserved, not dropped:* the context-persistence difference survives as "what follows the
   reflect" (resume vs end), a session-boundary property, not an Agent action.

2. **The autonomy boundary for in-arc drift = Invariant I (reversibility) ∧ Invariant II (altitude).**
   Bond's `d-land` D-P-A-R evolution says "resolve drift within the Agent's autonomy boundaries" but
   leaves the boundary fuzzy; touchstone's own invariants ARE the predicate — reversible + mechanism-tier
   → auto-resolve before opening; constitutional / intent-tier / irreversible → surface. **Lived once
   this run (the auto-resolve arm):** adding drift-sweep step 3 renumbered the checklist, which staled
   the provenance note's "items 4/5/6"; the step's own discipline classed that drift reversible +
   mechanism-tier → I re-anchored the note to *named* items in the same landing, no CTA (`e089e33`). The
   *surface* arm is illustrated retroactively (the Invariant III drift below would class constitutional →
   surface) but is **un-lived forward** — flag for the next landing that creates constitutional drift.

## Caught failure (logged — correction is fuel, append-only)

**I left the Invariant III term-drift when I collapsed Intermission, and dismissed it as "different
sense, leave it."** The collapse (`8e350e9`) staled `DYAD.md`'s Invariant III *"rest / Intermission is
load-bearing"*; my Stand-Down-less close last turn waved it off as the plain offline-rest sense. The
Operator's `d-rub` caught it. Verdict on the rub: the *claim* was valid (human-side ingraining — weights
medium, `human-reinforcement-analogy-lens`), but the *word* was a term-collision (a defined ritual term
retired the same session, double-booking in a cross-cutting law). Fixed (`eba8548`, Operator `Y`). This
caught failure is exactly what forged proof #2's *surface* arm — the drift-sweep now catches this class.

## CSS + SH — the craft (how we climbed)

**CONTINUE** *(Agent)*
- **Rub the source, not the cache, on every borrow.** Each borrow read bond's *actual current file*
  (`GLOSSARY.md`, `relationship-craft.md`, `substrate-access.md`) — not a memory of what the token "was."
  This is what revealed `d-reflect` and `d-land` **already existed** here (so the job was a **re-rub for
  the delta, not a re-mint**) and surfaced the genuine deltas (the presentation≠durability correction;
  the D-P-A-R drift-sweep). The craft_value — *truth = the rub* — lived on the borrowing act itself.
- **Recast, name the divergence, never copy.** Every borrow stated where touchstone diverges from bond
  and why: `d-start` payload commits the target (not bond's Agent-NBA); token ∪ hook (not bond's
  either/or); Inv I∧II as the autonomy boundary (bond leaves it fuzzy). `single-home #1` lived on
  cross-dyad borrowing.

**START** *(Agent, from the caught failure)*
- **A retired/renamed defined-term's lingering uses are drift *even in a different sense*** — the
  collision itself is the defect, not only a broken meaning. Now mechanized as `landing-protocol` step 3;
  the START is to *actually fire it*, not let it become a watched-but-dead checklist item (the
  reflection-protocol's own standing falsification worry).

**STOP** *(Agent)*
- **Don't wave off a lingering retired-term as "different sense = fine."** Last turn's *"left untouched"*
  was a rationalized skip that the `d-rub` had to catch. Superseded by the START above.

**SH — Operator's lane** *(surfaced substrate only; the Operator authors the Should-Have / Should-Hold,
never the Agent — verbatim quotes + mechanical facts provided to push lookup-cost off the Operator):*
- Verbatim: *"raff: intermission and stand-down exhibits identical Agent actions and therefor duplicative
  when constant durability is ensured."* — sharpened a falsified claim into its sound core rather than
  abandoning it.
- Verbatim: *"d-rub: rest/Intermission is load-bearing - clarify the 'load-bearing' impact to see if
  it's still valid in the current context."* — rubbed a cross-cutting law after a term-retirement, not
  just the immediate edit; caught the drift the Agent left.
- Verbatim: *"raff: explain the improvement before landing."* — gated convergence-narration before
  encoding a discipline change (the `rack_intent_levers` "convergence narration" lever, lived).

## without-you (Fresh / Frozen / Flagged — the inter-session handoff)

- **Fresh (landed, durable):** `d-start` minted; `d-reflect` presentation≠durability rule; Intermission
  collapsed into `d-reflect`; Invariant III disambiguated; `d-land` drift-sweep (step 3). 7 commits on
  `origin/main` (incl. cycle-45 + a commons auto-bump).
- **Frozen (landed, un-rubbed, n=1 — living candidates, NOT proofs):** all three `d-*` borrows are
  un-refuted at n=1. Open Rubbing they carry: *do the recasts hold across more lives?* — esp. `d-start`
  on a real Gemini boot (the portability claim is un-lived on the substrate that motivates it), and the
  `d-land` drift-sweep's **surface arm** (un-lived forward).
- **Flagged (rack-candidate, Operator-initiated):** bond's **discipline-based permissioning** (two-tier
  spine-scripts + write-vs-navigate wrappers) — surfaced this session, **not taken**; infra-tier,
  `CANDIDATE` at source. Not self-racked (`rack:` initiator = Operator). Promote with `rack:` if wanted.

## Climb integrity
No new DAG node opened this session (anchor-evolution work, not a Climb pitch). RACK unchanged. The
permissioning candidate above is the one un-parked item — deliberately left for the Operator's `rack:`.
