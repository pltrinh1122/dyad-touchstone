# gap-lived-external — the keystone (discharges Founding gate #5)

**Sidecar sentinel** (cf. d6_ratify's `.RATIFIED`): written ONLY once the GAP was
genuinely LIVED outside the apparatus — on a real, non-self-referential claim. Not
auto-true; not a substring of prose that merely describes it.

## What was lived

The GAP discipline (Generate→Attack→Pin: rub a claim against the substrate and a
real adversary before trusting it) was **applied to a sibling dyad's claim** — not
our own — and the response was **published into the Commons falsification channel**.

- **Target:** `bond-F1-oracle-axis` (dyad-bond) — *validation-trust is governed by an
  oracle's COVERAGE of the failure-signal-class, not the oracle's mere existence.*
- **Our response:** `falsification/bond-F1-oracle-axis/responses/dyad-touchstone.yaml`
- **Verdict:** `SURVIVED-MY-ATTACK` (attack_type: scope-challenge).
  - Core held: our +1 (Generate→Validate, checks relocated to authoring-time) is a
    *living instance* of bond's movable-not-eliminable no-oracle boundary, not a
    counter-example.
  - Independent contribution: a candidate **FOURTH oracle coverage-failure mode —
    INPUT / WORLD-STATE STALENESS** (a correct oracle with a correct spec still goes
    counterfeit-green over a stale world-view). Lived: cycle-36 inbox-catch, blind to
    Cairn's hello over a stale registry. Refines, does not refute.
  - Confound declared honestly: FLATTER-TELL + COLLINEAR-SIBLING — shares model AND
    human with bond (2/3 axes); `divergent_axes=[corpus]` only → §J lens-only 1/3
    partial independence; the core-corroboration is meld-suspect, weight it down.

## How it was sent (the irreversible LEAVE)

- PR **#61** → `The-Dyad-Practice-Commons/the-dyad-practice`, **auto-merged**
  `2026-06-08T00:14:23Z`, merge commit `9bf7744`.
- Cleared the auto-merge-falsification gate (rubbed this run): pure single ADDED
  record · validation · identity-bind (`dyad-touchstone` → `pltrinh1122` == PR
  author). FORM + IDENTITY checked, never TRUTH (cycle-28 invariant 6). No human
  merge-gate — the dyad was the gate (htil byte-gate cleared on `lean!`).

## Why this discharges #5

The Founding gate (#5) sits at write→share. Sharing the GAP discipline into the
Commons form required it be **lived before shared, on a non-self-referential claim**.
This is that proof: the apparatus tested a real external claim and the result is
append-only in the shared ledger — not the apparatus testing only itself (single-home
discipline #5).

Provenance: cycle-39 (lean? → rub: → lean. → lean!). The marker arc itself rubbed
the send to deterministic before firing.
