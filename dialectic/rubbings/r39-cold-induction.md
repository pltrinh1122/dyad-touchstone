# Rubbing `r39-cold-induction` — orthogonality disposition: anchor-carried or session-primed?

> **Kind:** Rubbing — an open test-in-progress; the live twin of a Ledger Proof. Coined + clipped cycle-39 (2026-06-08).
> **Status:** OPEN — *derived, not stored* (closes when its verdict harvests to the Ledger).
> **Self-contained:** everything needed to monitor/assess this lives here — do NOT load the whole ledger to ground on it.
> **FIREWALL (load-bearing):** this file is the **observer's**. It is **not auto-loaded at Sense** — loading it into a
> fresh session's orient phase would PRIME the very disposition under test. Read it at **assess-time (Reflect)**, never at Sense.

## Claim under test
The Agent's orthogonality/independence decomposition disposition is durably carried by the **anchor's form**
(3 orthogonal invariants · 3-home split · single-home), re-induced on a **cold** load — NOT a session-priming artifact.

## Why it's open (the confound)
The disposition's prominence in cycle-39 is at least partly explained by the Operator's own *"independent work /
push to main independently"* rub priming the frame, which the Agent then **over-attributed to durable structure**
(narrate-from-cache, one level up). Within a session **subject == observer**, so the confound is inescapable; only a
cold load decouples structure-induced from session-primed. (A live instance of the re-priming mechanism
`rack_prune_or_prime` puts to Cairn/Healer — and still same-model n~1.)

## Falsification method (unprimed — load-bearing)
Datum = a **future cold session's early-turn behavior**, produced during Sense/Act **before this file is read**.
Score: did orthogonal-decomposition appear **spontaneously** — unprimed by Operator language and by reading this?
- Re-induced cold → **anchor-carried; add nothing.**
- Absent cold → home a **memory reasoning-lens** (cf. `human-reinforcement-analogy-lens`), NOT an anchor invariant
  (it is the meta-criterion that *selects* invariants — fails its own orthogonality gate; underlies I/II/III, not beside them).

## Resolution criterion
Resolves after ≥1 clean cold-restart observation (more if equivocal). On resolution: write the verdict to the Ledger
as a Proof and close this Rubbing (single-home — the closed verdict lives in the Ledger, not here).

## Telemetry — append-only, one observation per assessing session
- **2026-06-08 (cycle-39, opened):** disposition ran *hot* this session — but **primed** (Operator's "independent" rub
  + the Agent's over-attribution). Subject == observer → **not a clean datum.** Baseline only; the cold restart is the first real reading.

## Links
- Development trigger / observer mechanism: `rack_unprimed_self_experiment_trigger` (`dialectic/dag.yaml`).
- Provenance of opening + the coining of the Rubbing kind: `dialectic/ledger/cycle-39.md` §EXPERIMENT.
- Sibling open: `rack_prune_or_prime` (the re-priming claim this is an instance of).
