---
origin: "sha256:49bcbe1d…a91a"
unit-kind: "playbook"
schema-version: "discipline-ontology@2026-05-31"
lineage: "none"
trigger: "any Validate-family ask — orient, verify, corroborate, or attack a claim or state — where the depth of rigor the ask actually needs is not fixed in advance"
claim: "consolidating the three G0 Validate mechanisms (Falsify · Triangulate · Ground) plus plain orientation (Read) into ONE Agent-escalated token — depth chosen by checking the ask, not by which word was typed — improves coverage (Triangulation stops being invisible, Grounding gets named) AND ergonomics (one word, not four to pick between) more than it costs in friction on the lightweight case"
refutation: "the Agent consistently over- or under-escalates without being told which rung · the plain-orientation case feels heavier than bare read: did, so the Operator types something more verbose than d-rub just to ask for state · a rung is admitted with no lived proof (professed, not held) · escalation-depth is claimed but never actually walked in one pitch"
mechanism: "the G0 Validate trio (falsify · triangulate · ground) + agent-determined depth"
---
# d-rub — the Validate-family, one escalating token

## Index Line
> **d-rub:** When you need to check something against reality — orient to state, verify a claim, corroborate an answer, or attack a stated move — fire **one** token, `d-rub`, and let the **Agent escalate to the depth the ask actually needs**, not the depth a chosen word pre-declares. G0's Contract names three orthogonal Validate mechanisms — **Falsify** (attack a claim; keep what survives), **Triangulate** (reach the answer two independent ways; compare), **Ground** (test an assumption against reality, *by execution*) — plus plain **Read** (orient; report re-derived state; do not act). d-rub is the umbrella: **Ground → Read → Triangulate → Rub**, one job, escalating rigor, Agent-determined depth (mirroring `d-land`'s own precedent: *"scope is Agent-determined by checking state, not by which word was typed"*). A plain status check stops at **Read**; an adversarial test walks to **Rub**. The Agent **names which rung(s) it ran**, so the escalation stays reviewable. The bind: rigor is *matched to the ask, this run*, never narrated from a stale cache and never fixed by vocabulary in advance.

## The Move *(ordered, Agent-determined depth)*
1. **Read the ask, not the word.** Determine how far up the ladder this ask needs to go by checking the state it concerns — a "just tell me where X is" stops at Read; "is this actually true?" needs Ground; "does this survive attack?" needs Rub. Depth is Agent-selected, then **named**.
2. **Ground** — establish a claim or assumption against reality **by execution**: run the actual command, check the actual state. Never narrate from memory or a stale cache. (The rung the escalation rests on — every heavier rung's premises must be grounded this-run first.)
3. **Read** — orient: report the re-derived state. **Do not act.** The lightest rung; most asks stop here. Core behavior carried unchanged from the retired `read:` marker.
4. **Triangulate** — reach the same answer two independent ways and compare; used when a single grounding pass doesn't earn enough confidence, or corroboration itself is the point.
5. **Rub** — attack a **stated** move or claim; try to break it. **Bounded to that target**, never the whole situation (the anxiety-grain guard). Firing this rung **pre-authorizes the Operator to bear re-alignment** — the in-moment exception to the no-contest law; a *solicited* contest defuses rationalization-defense.
6. **Name the rung(s) run.** The transparency that keeps escalation-depth reviewable — the same role a provenance tag serves elsewhere. A claim asserted after a rub cites a *this-run streak*, not a cached shine.

## Two boundaries preserved from the retired markers
The consolidation **retired** `read:` and `rub:` as separate tokens but **kept** their hard-won boundaries:
- **Fact-check vs. attack (cycle-39).** Verifying a stated claim against its source is **Ground+Read**, not Rub — it does not need the whole ladder. Previously "which marker do I pick"; now "how far did this ask escalate" — same distinction, reframed as depth within one job.
- **The anxiety-grain over-guard.** The unbounded "falsify everything" reading was never licensed by `rub:` and is not licensed by `d-rub`: even the full ladder stays scoped to the *stated* target. Escalation-depth is not permission to attack unbidden.
- **`introspect:` stays separate.** Examining the Agent's own cognition is **not** a d-rub move — there is no external substrate to query, so its outputs are self-report, not a grounded verdict.

## Admitted rungs *(one per lived proof — all four earned)*
- **Ground** — *establish against reality, by execution, before asserting.* **Proof:** cycle-47 (under the escalating rub, *every* premise claim was grounded this-run before assertion — `fr.yaml`, `cycle-43.md:67-71`, `gap-lived-external.md`, the response count — the attack cited streaks, not shine).
- **Read** — *orient; report re-derived state; do not act.* **Proof:** lived every session as the default rung — this session's Stand-Up frontier read (orient to the computed frontier, report, do not act) is a textbook instance; core behavior carried unchanged from `read:`, touchstone's single most-used marker.
- **Triangulate** — *reach the same answer two independent ways and compare.* **Proof:** cycle-50 (this Playbook's own provenance rub — the claim *"was Triangulate ever lived?"* answered two **independent substrates**, working-tree `grep` ∥ full-history `git pickaxe`, which *could have diverged* and instead converged; corroboration was the point, not decoration). **The rung earned its first proof by being used to investigate whether it had one.** N=1, same Operator/Agent pair.
- **Rub** — *the escalating attack on a stated target.* **Proof:** cycle-45 (a `d-rub` on Invariant III caught a retired-term drift — "Intermission" left double-booked in a cross-cutting law after its same-session retirement — that the Agent had waved off as "different sense, leave it"; the rub's verdict split *claim valid / word a collision*, catching what pure-G rationalized past) + cycle-47 (an escalating `d-rub` on `gap_rings_sound` walked present-target → falsify-premise → Operator `clip: retracted`, catching a three-cycle-stale climb node in a single pitch).

All four rungs now carry a lived proof — **none remain named-but-unwritten.** The frontier turned outward by ascending, not by cataloguing.

## Invariants
- **Rub the substrate this run, never memory** — every rung establishes state by query, not by narration (the Ground floor under all four).
- **Depth is matched to the ask, not fixed by the word** — Agent-determined escalation, then named for review (cycle-43).
- **The heavier the rung, the tighter the bound** — even the full attack stays scoped to the *stated* target; unbounded falsification is over-guard.
- **A rung is admitted only on a lived proof** — all four now earned (Triangulate last, cycle-50); still re-rubbed each pitch.
- **Soundness is the gate, not breadth** — a proof that survives falsification is codifiable at N=1; breadth is provenance to disclose, never a bar.
- **Un-refuted is not verified** — the consolidation's own claim is re-tested each session it is used.
- **A Ledger of proofs precedes a Playbook** — this Playbook admits only the rungs the Ledger has already earned.

## Ledger
The accumulating evidence lives in **`ledger/`** — **one testimonial per contributor** (`<contributor-sha8>-<label>.md`), append-only, each pinned to a `PLAYBOOK.md` version. Weight counts **distinct contributors** (breadth); depth counts survived attacks within a file. Seed: **`ledger/49bcbe1d-seed.md`** (dyad-touchstone) — the attacks `d-rub` has survived across our climb (internal provenance cross-referenced for traceability: `dialectic/ledger/` cycles 43/45/47/50 — cited, not required to read the account).

> **Provenance & status.** *Soundness is the gate — not breadth: a proof that survives falsification is admissible at N=1, re-rubbed each pitch.* Authored **self-contained** per CONTRIBUTING.md §2, but **not yet submitted** to the Commons — admission is FO-gated at the `write → share` leave (Founding gate #5), a separate act. **Provisional — single-dyad (dyad-touchstone), seeking a second contributor.** What did **not** yet close: the **consolidation's own claim** — whether the Agent escalates to the *right* rung **consistently and unbidden**, and whether plain-orientation feels heavier than bare `read:` did — un-refuted, **not** verified, re-tested each session `d-rub` is used.
