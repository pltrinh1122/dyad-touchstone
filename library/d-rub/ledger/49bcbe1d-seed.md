# Ledger seed — dyad-touchstone (n=5) — d-rub

> **One file = one contributor's testimonials.** Append-only; never edited by another dyad (the
> conflict-free consumer grain). contributor = `sha256:49bcbe1d…a91a` (dyad-touchstone).
> `pinned: c17fd3da09b7a8e0b729f409f13844d29511d3f3` — every entry below pins to this `PLAYBOOK.md`
> blob (per CONTRIBUTING.md §2, the pin binds this version; re-binds + CI-re-verifies at the
> Commons `write→share` PR if the body is revised). Internal provenance (cited for traceability,
> not required to read this account): `dialectic/ledger/` cycles 43 (adoption) · 45 · 47 · 50.

Each entry = a survived dyadic-cycle (a real attack the discipline survived, none refuting):

1. **Rub caught what pure-G rationalized past** *(Rub · cycle-45)* — a `d-rub` on a cross-cutting
   invariant caught a retired-term drift the Agent had waved off as *"different sense, leave it."*
   The rung's verdict split *claim-valid / word-a-collision*, surfacing a real staleness a
   generate-only pass had skipped. The Rub rung held.

2. **Escalation retracted a stale node in one pitch** *(Rub · Ground · cycle-47)* — an escalating
   `d-rub` walked present-target → Ground (every premise established against the substrate this-run)
   → falsify-premise → retraction, killing a three-cycle-stale climb node in a single pitch. The
   Agent-determined depth (one token, no rung named by the Operator) escalated correctly under real
   load.

3. **The discipline caught its own violation** *(Rub · Ground · cycle-50)* — attack: the
   discipline's maturity was asserted from a *stored* "n=0" status label. A `d-rub` grounded that
   claim against the cycle-ledger this-run and refuted it (d-rub had lived since adoption). The
   discipline survived by catching a violation of its **own** core invariant — *status is never
   stored, only rubbed* — held even when the stale cache was its own status field.

4. **Triangulate survived the wu-wei attack — self-exemplifying** *(Triangulate · cycle-50)* —
   attack: *"Triangulate was never lived; it's invented vocabulary wu-wei should prune."* Rubbed two
   independent substrates (working-tree `grep` ∥ full-history `git pickaxe`) that could have
   diverged and converged: the rung is G0-inherited *nature*, not invented — surviving the attack.
   The provenance rub **was itself** the Triangulate rung: the rung earned its first proof by being
   used to investigate whether it had one.

5. **Soundness, not breadth, is the admission gate** *(falsify-and-codify · cycle-50)* — attack:
   *is a rung admissible at N=1, single-dyad?* Survived: a proof that survives falsification is
   codifiable at N=1, re-rubbed each pitch; breadth is provenance to disclose, never a bar. All four
   rungs admitted one-per-proof — Ground (c47) · Read (lived each session) · Triangulate (c50) ·
   Rub (c45/c47).

**Pattern:** the discipline survives by *making rigor match the ask, this run — never from a cache*,
and survived even when the cache was its own stored status label. **Open, not refuted:** the
consolidation's own claim (the Agent escalates to the *right* rung *consistently and unbidden*;
plain-orientation ergonomics vs bare `read:`) has **not** run to a verdict — un-refuted, not
verified. n=5, single-dyad (breadth pending a second contributor via Participate).
